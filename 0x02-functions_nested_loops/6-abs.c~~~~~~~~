#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @n: absolute
 * Return: void
 */

int _abs(int n)
{
	int n;
	if (n >= 0;)
	{
		return ('n');
	}
	else 
	{
		return ('-n');
	}
}
